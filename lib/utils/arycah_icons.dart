import 'package:flutter/cupertino.dart';

import '../library_imports.dart';

class ArycahIcons {
  static const arrowLeft = Icon(IconlyBold.arrow_left, color: Colors.black);
  static const send = Icon(IconlyBold.send, color: Colors.black);
  static const chat = Icon(IconlyLight.chat, color: Colors.blueAccent);
  static const search = Icon(IconlyBold.search, color: Colors.blueAccent);
  static const filter = Icon(IconlyBold.filter, color: Colors.blue);
  static const bookmark = Icon(IconlyBold.bookmark, color: Color(0xFF8A8B8F));
  static const circleSharp =
      Icon(Icons.circle_sharp, color: Colors.blue, size: 15);

  static const add = Icon(CupertinoIcons.add);
  static const tickSquare =
      Icon(IconlyBold.tick_square, color: Colors.blue, size: 16);
  static const menu = Icons.menu;
  static const notification = IconlyBold.notification;
  static const swap = IconlyBold.swap;
  static const heart = IconlyBold.heart;
  static const bookmarkSingle = IconlyBold.bookmark;
  static const playCircle = Icon(Icons.play_circle, size: 15);
  static const arrowForward = Icon(Icons.arrow_forward_ios_rounded, size: 15);
}
